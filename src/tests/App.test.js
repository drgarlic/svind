test.todo('Need to find a fix for jest parsing svelte-spa-router')

// import '@testing-library/jest-dom/extend-expect';

// import { render } from '@testing-library/svelte';

// import App from '../App.svelte';

// describe('App', () => {
//     it('should render', () => {
//         expect(render(App))
//     })
// })

